name: Pixel Schema Validation

on:
    push:
        branches: [main]
    pull_request:
    
jobs:
    pixel-validation-nousermap:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: 'npm'
            - run: npm run install-ci
            - run: npm run validate-pixel-defs

    pixel-validation-withusermap:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: 'npm'
      
            - name: Checkout internal-github-asana-utils repo
              uses: actions/checkout@v4
              with:
                  token: ${{ secrets.DAXMOBILE_TOKEN }}
                  path: internal-github-asana-utils  
                  repository: duckduckgo/internal-github-asana-utils
                        
            - run: npm run install-ci
            - run: ls -la ./internal-github-asana-utils/user_map.yml
            - name: Debug - Show what the npm script will run
              run: npm run validate-pixel-defs-usermap --dry-run 2>/dev/null || echo "Command: validate-ddg-pixel-defs ./pixel-definitions -g ./internal-github-asana-utils/user_map.yml"
            - name: Debug - Test the command directly
              run: npx validate-ddg-pixel-defs ./pixel-definitions -g ./internal-github-asana-utils/user_map.yml

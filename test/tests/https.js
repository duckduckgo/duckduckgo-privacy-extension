(function() {
  QUnit.module('HTTPS')
  
  QUnit.test('test https upgrade rules', function (assert) {

      var done = assert.async()
      bkg.db.ready().then(() => {
          debugger
      })

  })
})()

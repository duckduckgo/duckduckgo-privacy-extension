{
    "atb": {
        "key": "atb",
        "type": "string",
        "pattern": "^v[0-9]{1,2}$|v[0-9]{1,3}-[0-7]([a-z]{2})?",
        "description": "Add to browser value, corresponding to the users install week and day"
    },
    "appVersion": {
        "key": "appVersion",
        "type": "string",
        "description": "Extension version, but sent with 'appVersion' key.",
        "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
        "examples": ["2024.10.16"]
    },
    "extensionVersion": {
        "key": "extensionVersion",
        "type": "string",
        "description": "Current version of the extension.",
        "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$",
        "examples": ["2024.10.16"]
    },
    "remoteConfigEtag": {
        "key": "remoteConfigEtag",
        "description": "",
        "pattern": "^$|null|W/\"[-0-9a-f]+"
    }
}
